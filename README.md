# People Picker
This is the repo for the people picker project

# Run app
````bash
cd server
node server.js
````

````bash
cd vanillaClient
npx browser-sync --files "*.html, *.js, *.css" --server start
````
