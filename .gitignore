node_modules
.vscode

/data/db 
//TODO: ignore the files inside /data/db but not the folder itself. The below should be working but isn't:
// /data/db/*   // ignore the files inside of data/db ...
// !/data/db   // ... but not the folder itself